apiVersion: v1
kind: Secret
metadata:
  name: baileys-secrets
  namespace: baileys
type: Opaque
stringData:
  # 64 hex characters master key for encryption
  # Generate with: openssl rand -hex 32
  master-key: "REPLACE_WITH_YOUR_64_CHAR_HEX_MASTER_KEY"
